Two python scripts (kd_tree.py, lsh.py) have been created with the classes KDTree and LSH.
An example of how to use these classes to perform range search and return the N most similar reviews is given in example.py.

To install the required dependencies run:
pip install -r requirements.txt
